
name: Build

on:
  push:
    branches:
      - main


jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis
      - name: build and analyze with sonarqube
        uses: sonarsource/sonarqube-scan-action@master
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}

      - name: install npm dependency
        run: npm install

      - name: docker login 
        run: login -u ${{secrets.DOCKERHUB_USERNAME}} -p ${{secrets.DOCKERHUB_TOKEN}}

      - name: docker build and push
        run: |
          docker build -t hotstar .
          docker tag hotstar rahul70/hotstar:latest
          login -u ${{secrets.DOCKERHUB_USERNAME}} -p ${{secrets.DOCKERHUB_TOKEN}}
          docker push rahul70/hotstar:latest
  deploy:
    needs: build-analyze-scan
    runs-on: [self-hosted]
    steps:
      - name: docker pull image
        run: docker pull rahul70/hotstar:latest
      
      - name: deploy to container
        run: docker run -d --name hotstar -p 3000:3000 rahul70/hotstar:latest

      - name: update update-kubeconfig
        run: aws eks --region us-east-1 update-kubeconfig --name EKS_CLOUD

      - name: deploy to kubernetes
        run: kubectl apply -f deployment.yml

      - name: deploy service
        run: kubectl apply -f service.yml


 
    
      
